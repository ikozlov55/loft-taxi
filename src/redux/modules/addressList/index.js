import reducer from './reducers';

import * as addressListSelectors from './selectors';
import * as addressListOperations from './operations';
import * as addressListTypes from './types';

export { addressListSelectors, addressListOperations, addressListTypes };

export default reducer;
